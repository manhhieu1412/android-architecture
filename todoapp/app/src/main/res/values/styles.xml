<resources>

    <style name="RecyclerView">
        <item name="android:clipChildren">false</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:smoothScrollbar">true</item>
        <item name="android:drawSelectorOnTop">false</item>
    </style>

    <style name="ListView" parent="android:Widget.ListView">
        <item name="android:divider">@color/divider</item>
        <item name="android:dividerHeight">1px</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:drawSelectorOnTop">false</item>
        <item name="android:smoothScrollbar">true</item>
        <item name="android:listSelector">@color/transparent</item>
        <item name="android:cacheColorHint">@color/transparent</item>
    </style>

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorAccent">@color/white</item>
        <item name="actionMenuTextAppearance">@style/MyMenuTextAppearance</item>
    </style>

    <style name="Toolbar.Grey" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:background">#FAFAFA</item>
    </style>

    <style name="Toolbar.White" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorAccent">@color/white</item>
        <item name="android:background">@color/white</item>
    </style>

    <style name="MyMenuTextAppearance" parent="TextAppearance.AppCompat.Widget.ActionBar.Menu">
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
